$schema: "https://moonrepo.dev/schemas/project.json"

tasks:
  build:
    deps:
      - "wasm-loader:build"

  transpile:
    command: "node ../../scripts/transpile.mjs git-module"
    inputs:
      - "/target/wasm32-wasip2/release/git_go.wasm"
      - "/scripts/transpile.mjs"
    outputs:
      - "wasm"
    deps:
      - "git-cli-wasm:copy-to-target"

  transpile-sync:
    command: "node ../../scripts/transpile.mjs git-module --sync"
    inputs:
      - "/target/wasm32-wasip2/release/git_go.wasm"
      - "/scripts/transpile.mjs"
    outputs:
      - "wasm-sync"
    deps:
      - "git-cli-wasm:copy-to-target"
