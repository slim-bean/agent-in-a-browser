[package]
name = "codex-login"
version = "0.0.0"
edition = "2024"
description = "Stub for codex-login in wasip2 — auth types with no-op implementations"

[lib]
name = "codex_login"
path = "src/lib.rs"

[dependencies]
async-trait = "0.1"
reqwest = { path = "../wasi-reqwest" }
schemars = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
