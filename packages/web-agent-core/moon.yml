$schema: "https://moonrepo.dev/schemas/project.json"

tasks:
  build:
    script: "tsc"
    inputs:
      - "src/**"
      - "package.json"
      - "tsconfig.json"
    outputs:
      - "dist"
    deps:
      - "wasi-shims:build"
